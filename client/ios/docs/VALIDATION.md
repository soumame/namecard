# 実装時の検証記録

検証日: 2026-09-06〜08。ユーザーによる実機試験でSTATUS・チェック柄更新は動作報告があり、
作成画像も通信修正後の再試験2回目に更新成功の報告を受けました。
1回目は約4,000 bytes送信後に「送信内容の不一致」とのエラーがあり、原因はログ未取得のため未特定です。
その後、URL書き込み・クリーニングの動作確認報告も受けました。
以前はCode 100後の手動やり直しで成功する報告がありましたが、その後の3試行では
頻繁な切断、送信後のisAvailable=false、ST25 ADの102／0f、再スキャン後の転送位置不一致、Code 203が報告されています。
再開不能などの追加修正後、転送・復帰が大幅に改善し、切断による中断がなくなったとの報告を受けました。
連続2回目は表示が更新済みでもアプリの完了確認が未完了でした。最新のACKログは未取得で、原因は未特定です。
続く報告では当て方による不調・切断が残る一方、処理のスタックは見られないとのことです。
最新ビルドの使用と完了確認警告の解消は未確認です。成功率・時間は未計測で、給電計測・各経路10回連続成功は未確認です。
以下の成功はソフトウェアの検証結果であり、署名・TestFlightの審査やApp Storeの実機公開条件を満たした意味ではありません。
ベータ配布では試験対象に仮の時間予算を使い、App Store版の実測記録とは区別します。

## 要素回転スナップ追加の検証（2026-09-13）

- iOS：アプリ・テストターゲットのビルドと単体63件、回転スナップ切替のUIテスト1件が成功。
- Android：単体49件、Lint、Debug／テストAPKビルド、エミュレーターでの描画テスト8件が成功。
- 画像とテキストの15°間隔・±4°吸着、微小回転の累積と吸着からの離脱、±180°付近、
  ジェスチャー終了・トグル切替・別レイヤーへの状態持ち越し防止、Undo／RedoとBINへの反映を確認。
  要素回転・表示回転・位置のスナップ設定は独立し、初期状態では要素回転スナップはOFF。

## フォント・表示回転スナップ追加の検証（2026-09-13）

- Xcode 26.6でアプリ・単体テスト・画面テストの各Simulatorターゲットをビルド成功。
  ソースやアセットの除外、一時的な実装の置き換えは行っていない。
- iPhone Air／iOS 26.5 Simulatorでアプリ単体61件が成功（EditorTestsは15件）。
  フォント・太字・斜体・下線の白黒／4階調BINへの反映、書式のUndo／Redo、
  日本語だけのテキストでもシステムフォントの斜体が反映されることを確認。
  システム・Hiragino・Courier・Timesの24pt／96ptで、描画画素が選択範囲内に収まることも確認した。
- 表示回転スナップは15°間隔・4°以内の吸着、1°ずつの連続入力による吸着からの離脱、
  ±180°付近の正規化、同時拡縮・移動での指の中心位置の保持、初期OFFと位置スナップからの独立、
  切替・リセット、および表示変換がBINに影響しないことを単体テストで確認。
- 追加した画面テスト2件が成功。B／I／U切替・OSフォント検索と選択・追加・Undo／Redo・完成プレビュー、
  表示回転スナップのON／OFFと位置スナップからの独立を確認。
  キーボード表示中の書式フォームとフォント選択後のプレビューをスクリーンショットで目視確認した。

最初の描画試験で日本語フォントの下線と代替フォントの字形が選択範囲からはみ出すことを検出した。
測定と描画で行・行間の設定を統一し、字形の実測範囲・斜体の傾き・代替フォントの行間を
選択範囲に反映して修正した。システムの斜体書体だけでは日本語の代替フォントが傾かないため、
斜体は全字形に共通の傾きを適用する。プロジェクト生成の`--check`も成功。

## URLクリア追加の検証（2026-09-11）

- Android: JVM単体テスト38件、Lint（エラー0、既存警告8）、Debug APKビルドが成功。
  空欄入力の拒否と、短い空NDEFの読み返しで旧URLの残存バイトを含めないことを確認。
- NamecardCore: Swift Package Managerの59件が成功。クリア用の空NDEFとType 5の書き込み計画を確認。
- iOSアプリ単体テスト50件が成功（URLWriterTests 16件を含む）。Core NFCが空レコードとして
  解釈しURIを返さないこと、再クリア・URL再設定、書込失敗・読み返し不一致・Mailbox再開失敗の復旧、
  別UIDや別操作への置き換え拒否、保護領域・未知形式の拒否、初期化中断、旧復旧記録とアプリ再起動後の復旧を確認。
- iOS画面テスト2件が成功。空欄のままクリアできること、通常書き込みは空欄で無効なこと、
  設定・クリアともフォームを閉じてからNFC進捗へ移ることを確認。

この端末はXcode 16.4／iOS 18.6 Simulatorであり、リポジトリ全体の通常ビルドは既存の
Liquid Glass API（Xcode 26以降）が解決できず失敗した。アプリ単体・画面テストは`/tmp`の一時コピーで
`LiquidGlassSupport.swift`の既存iOS 17〜18向け分岐だけをコンパイルして実行した。
URL処理・画面・テストは今回のソースそのものを使用し、リポジトリのLiquid Glass実装は変更していない。
この結果はXcode 26での通常ビルド成功を示さない。プロジェクト生成の`--check`は成功。
URLクリアの実機RF確認は未実施。[実機確認項目](HARDWARE_VALIDATION.md)の設定→クリア→画像更新→再設定を確認する。

## QRコード追加の検証（2026-09-11）

- Android: JVM単体テスト42件が成功。通常のURL、日本語パス、既存エスケープを含むURLについて、
  生成したプレビュー用・名刺用ピクセルをZXingで読み取り、正規化済みURLと一致することを確認。
  4セルの白い余白、白黒のみの画素、過密QRの追加禁止、不正URLの拒否も確認した。
  長いURLの生成画像はカメラの歪みがないため`PURE_BARCODE`で検証した。
  Lintはエラー0、既存箇所への警告9件（Gradle更新通知を含む）、Debug APKビルドも成功。
- iOS: アプリ単体54件が成功（QRコード関連4件を含む）。Core Imageの読取器で生成画像と
  PNG往復後の画像からURLを復元。キャンバスへ追加して白黒・4階調BINへ変換した後も
  元URLを読み取れ、整数ピクセル配置とUndo／Redoが維持されることを確認。
- iOS画面テスト4件が成功。QR生成プレビュー・名刺へ追加・Undo／Redo、入力変更で古いプレビューを
  消すこと、および既存URL設定・クリア画面を確認。iPhone 16e／iOS 18.6 Simulatorのプレビュー画像も目視確認した。

iOSは上記URLクリア検証と同じXcode 16.4環境のため、一時コピーで既存のiOS 17〜18向け表示を使用した。
最初の画像読取試験はSimulatorのVision推論環境を初期化できず失敗し、Core Image読取器に変更して成功した。
QR生成・画面・エディターのコードはリポジトリのソースそのものを使用した。
Xcode 26での通常ビルド、および電子ペーパー実物をカメラで読み取る試験は未実施。

## 自動テスト（2026-09-06〜08）

| 対象 | 実行環境 | 結果 |
|---|---|---|
| NamecardCore | macOS上のSwift Package Manager | 58件成功 |
| EditorTests | iPhone Air・iOS 26.5 Simulator、Debug／Beta | 各8件成功 |
| URLWriterTests | 同上、NFCを模擬通信へ差し替え | 9件成功 |
| ST25TransportErrorTests | 同上、Core NFCエラーを模擬 | 3件成功 |
| CancellableNFCRequestTests | 同上、SDK応答の遅延を模擬 | 3件成功 |
| NFCRecoveryPolicyTests | 同上、レジスタ一時エラー・時間を模擬 | 5件成功 |
| NFCHapticGuideTests | 同上、振動出力と通信を模擬 | 5件成功 |
| CoreGuideHapticsTests | 同上、エンジン起動・再生・中断を模擬 | 5件成功 |
| LibraryThumbnailTests | 同上、一時Libraryを使用 | 3件成功 |
| DistributionPolicyTests | 同上、ビルド条件・同梱設定を照合 | 各2件成功 |
| NamecardUITests | 同上、Debug／Beta | 各6件成功 |
| ProjectGenerationTests | macOS、Python標準unittest | 3件成功 |

ベータ準備時点でアプリ単体はDebug／Betaそれぞれ43件が成功しています。
配布区分と処理済みInfo.plistの一致、ベータ対象の絞り込み、通常版がベータ用の時間予算を使わないことを確認しました。
共通58件には、Betaでも更新時間＋5秒を確保できなければEXECUTEを延期し、
4階調表示済み／更新中断済みからの移行を拒否する確認を含みます。
通常／BetaのUI各6件はアセットを含むビルドで成功しました。Beta表示・白黒書き込みボタンの有効化と、
4階調書き込みの無効化、編集・保存・再起動後の読込、Undo／Redo、URLフォームを確認しました。
画面確認でベータ案内と上部操作バーの重なりを発見し、各NavigationStackのコンテンツ側へ案内を移しました。
修正後の既存UI5件が成功し、案内試験は各タブを別の識別子で確認するよう更新しました。
safeAreaInsetのテキストはテストAPIの座標が実表示と一致しなかったため、座標比較は採用せず、
保存したスクリーンショットと操作ボタンの利用可否で確認します。
以前のiOS 17.5での結果は下記の履歴に残しています。

共通テストでは、Android実コードから作成した白黒／4階調BINとの全バイト一致、透過合成、
native座標・MSB first、量子化境界、CRC・ACK検証、URL正規化、NDEF／TLV長の境界を確認しています。
転送試験では128-byte DATA、VDD別の間隔、quiet時間、旧FW清掃、期限切れ、ACK喪失・重複、
MCU再起動、別UIDの拒否、不明なCOMPLETEの再送、10パターンの再開を模擬しています。
接続喪失対策として7件を追加し、DATA受信前／受信後の一時エラーと破損ACKの再送、
回数・残り時間の制限、COMMIT／EXECUTEをその場で再送しないこと、
タグ再検出のUID・回数・更新段階の制限を確認しました。

アプリ単体テストでは50操作のUndo上限、レイヤー操作、紙面表示・グリッドの出力除外、
BINの往復、画像の向き・サイズ・透過を確認しています。URLはPREPAREからMailbox再開までの順序、
非対応FW、読み返し不一致、復旧記録の保存失敗、工場初期状態の途中復旧、
別UID・保護領域・未知の変更・破損した復旧記録の拒否を確認しています。
Core NFCエラーの追加3件では、接続喪失、一時的なRFエラー、非対応コマンド・セッション終了・
キャンセル・Mailbox初期設定エラーを区別することを確認しました。
2026-09-08の最初の通信修正では共通44件とアプリ単体23件が成功しました。
4回の切断後も、MCUのRAM保持／消失の両方で元の画像を再開して1回だけEXECUTEすること、
セッション終了時にSDK応答を待たず処理を解放すること、遅れた成功／失敗応答を破棄することを確認しました。
後続の再開不能対策では、COMMIT ACK喪失と本体のpending Flash復元を模擬し、修正前の不一致を再現しました。
修正後は共通45件が成功し、新しい転送IDで元画像を再送して完了することを確認しています。
不正な前進位置や古いACKを拒否した後も、次のスキャンで不正な再開位置を持ち越さないことを確認しました。
NFC View分離後の画面テスト5件も再実行し、全件成功しています（約76秒）。
アプリ単体は28件が成功しています。追加の5件ではMB_CTRLの一時エラーの限定再試行、
回数・期限・キャンセルによる中止、Code 203後の有限の待機、システムシートの更新頻度制限を確認しました。
共通45件・単体28件・UI 5件が、再開不能・画面負荷対策時点の結果です。
後続の完了確認対策では、連続2回目の更新後にSTATUS応答だけを失うケースが修正前に失敗することを確認しました。
追加した4件を含む共通49件が成功しています。一時エラー・破損ACKの後に新しいSTATUSで完了すること、
更新命令を増やさないこと、最大3回・quiet・元の時間上限を守ること、実際の接続喪失では確認保留にすることを確認しました。
完了確認の修正後にアプリ単体28件も再実行し、全件成功しました。
完了確認対策の時点では画面テストは再実行せず、View分離時の5件を維持しました。
後続の触覚案内の試作では、追加の共通8件を含む57件とアプリ単体32件が成功しました。
電圧・応答時間・応答の鮮度、安定の判定と振動頻度制限、EXECUTE応答喪失時の事前停止、
設定OFF・終了・新しい接続への切り替え、古い応答の破棄、通信内容・回数・タイムアウトを変えないことを確認しました。
この試作時点では振動の実出力やNFCシートとの共存は未確認でした。設定保存用UserDefaultsの利用理由CA92.1をPrivacyInfo.xcprivacyへ追加しました。
触覚案内のSettings追加後に既存UIテスト5件も再実行し、全件成功しました（約77秒）。
初回のUIKit版について書き込み中に振動しないとの報告を受け、出力をCore Hapticsへ変更しました。
振動判定の閾値は維持しています。追加の6件を含むアプリ単体38件が成功し、非同期起動中の取消し、
古い接続の起動通知・中断通知の排除、古い再生要求の破棄、起動の有限回数、エラーと要求数のログを確認しました。
共通57件・UI5件は初回試作時の結果です。今回は共通処理と画面構成を変更しておらず、再実行していません。
UIKit版の無振動の原因はまだ特定できていません。Core Haptics版はその後、ユーザーから書き換え・振動とも正しく動作したとの報告を受けました。
試行回数や時間比較は未取得です。別途、テキスト操作時などの約3秒のHangが報告され、[処理時間の計測](PERFORMANCE.md)を追加しています。
Simulatorでは実際のタグ再検出・RF給電やiPhone XRのHangを試験できません。

Hang計測追加後、同じアセット除外条件のSimulator向けDebugビルド、アプリ単体38件、
既存UI5件がすべて成功しました（UI約75秒）。文字入力・Undo／Redo・保存／再読込・プレビュー・
URLフォームからNFC画面への遷移を確認しています。実機の約3秒の停止原因は未特定で、
この計測追加をHang修正完了とは扱いません。共通処理は変更しておらず、共通テストは再実行していません。

PR #4からの[選択統合](PR4_INTEGRATION.md)後は、共通57件、追加3件を含むアプリ単体41件、
UI5件がすべて成功しました（UI約83秒）。サムネイルを再読込・名称変更で再利用し、
BIN差し替え・破損・削除に追従すること、白黒／4階調の再起動後の一致、保存直後・再起動後の画面表示を確認しました。
Simulator向けDebugのビルドは同じアセット除外条件です。統合版の実機RF試験は未実施です。

Library統合時に実行した`generate_project.py --check`は失敗しました。その時点で変更していないXcodeプロジェクトに、
生成スクリプトと異なるobjectVersion・同期グループ属性・署名Team・互換設定・書式があります。
Library統合時にはプロジェクトの再生成を行いませんでした。
後続のベータ配布準備では、生成器を修正して署名・バージョンを保持して再生成しました。
`--check`は設定内容を照合し、Xcodeの書式差は許容します。署名保持・SDK別の署名指定の往復・
BetaとReleaseの混在検出・Archiveの構成を確認するPython試験3件が成功しています。
アプリのビルド・テスト成功と、GitHub上のCI全体の成功は区別します。

同日のAndroid側修正は、単体テスト33件（接続解放・探索再初期化の6件を含む）、
Lint（エラー0、既存警告8）、Debug APKビルドが成功しています。
繰り返す切断、背景化からの復帰、古い検出通知・タイマー・終了通知の排除、
URLを含む接続のキャンセル、接続開始とキャンセルの競合を模擬しました。
後続のAndroid連続書き込み修正では37件のテストとAPKビルドが成功し、ユーザーから
実機で改善したとの報告を受けました。確認端末・試行回数は未記録で、両Android機種での成功とは断定しません。
iOSの追加修正後の実機確認は、引き続きiPhone XR／iOS 18で行います。

画面テストでは日本語の追加・Library保存・再起動後の再編集／プレビュー、Undo／Redo、
4階調書き込みの無効表示、SimulatorでのNFC非対応表示、URL入力画面からNFC進捗への遷移を確認しました。
iOS 17で初回の入力確定が反映されない動作を修正し、テキスト・URL・Libraryの名称入力は
通常のフォーム画面へ統一しました。プレビューは画像を含むデータそのものから画面を開き、
初回の表示で画像が抜けないようにしました。画像要素の存在・高さをテストで検証し、
抽出したスクリーンショットでもプレビュー内の日本語を確認しています。
ホスト中断によるテストの時間切れは、実行中だけ`caffeinate`でスリープを抑止して再確認しました。
画面テストの実行時間は性能ベンチマークではありません。

## ビルド

2026-09-07に、ユーザーからアプリのビルド・起動・STATUS表示、その後チェック柄の更新が成功したとの報告を受けました。
作成画像は当初DATA中の接続喪失で失敗し、通信修正後の再試験では1回目に不一致エラー、2回目に更新成功の報告があります。
機種・OSは申告のみで実ログ未確認、scheme・ビルドログは未取得です。
以下はエージェントが行ったローカル検証の記録です。

Xcode 26.6（17F113）／iOS Simulator SDK 26.5（23F81a）で、Debug・Release・HardwareTestの
Swiftコンパイルとリンクに成功しています。アプリ単体テスト・UIテストのターゲットもビルドできています。
プロジェクト生成結果の一致、Info.plist・entitlements・PrivacyInfo.xcprivacyの構文も確認しました。

以前の検証時点ではSDKに対応するiOS 26.5 Simulator runtimeがなく、通常のschemeビルドと
`actool`のアセット生成が失敗しました。署名なしの実機向けReleaseビルドでも同じアセット生成エラーを確認しました。
runtimeのダウンロードは進行が停止したため中止しました。
当時は`EXCLUDED_SOURCE_FILE_NAMES=Assets.xcassets`をコマンド引数に指定した検証用ビルドを作成し、
ビルド済みテストをproject-freeな`.xctestrun`から既存のiOS 17.5で実行しています。
アセット除外はプロジェクトやCIへ設定していません。

2026-09-08のベータ配布準備時には対応するiOS 26.5 runtimeを確認できたため、アセットを除外せずに
通常ReleaseのSimulatorビルドと、Betaの署名なしiPhone向けArchiveが成功しました。
両方の成果物で配布区分・表示名・アイコン・NFC利用目的・PrivacyInfo・JSONリソースを確認しました。
HardwareTestのSimulatorビルドもアセットを含めて成功しています。
最初のDebugのschemeテストではアプリのx86_64ビルドと共通パッケージのarm64ビルドが混在して失敗したため、
Debug／HardwareTestのONLY_ACTIVE_ARCHをYESに揃えました。その後は通常のschemeテストで実行できています。
BetaのSimulatorテストもONLY_ACTIVE_ARCH=YESとし、配布用Archiveの最適化設定は維持します。
Apple署名・OrganizerのValidate App・アップロード・ベータ版の実機RF動作は未確認です。

触覚案内の試作でも、同じアセット除外指定でDebugのアプリ・単体テスト・UIテストのコンパイルとリンクを再確認しました。
加えてiPhoneOS向けHardwareTest構成を署名なし・同じアセット除外指定でビルドし、成功しました。
更新したPrivacyInfo.xcprivacyのplist構文も確認しています。実機へのインストール・振動確認は行っていません。
Core Haptics版も同じ条件のSimulator向けDebugと署名なし実機向けHardwareTestでビルドしました。
ユーザーが設定したXcodeプロジェクトの署名設定は変更していません。

通常の環境では[READMEのコマンド](../README.md#開発環境)を使用してください。
`.github/workflows/ios.yml`にはmacOS上の共通テスト、アセットを含むRelease・HardwareTest・Betaビルド、
通常／BetaのSimulatorアプリ単体試験・UI試験、配布区分の確認を定義しています。
GitHub上の今回のCI実行結果はまだありません。

## 実機・公開までに残る作業

1. ユーザーが成功したビルド環境・設定と通常ビルドのログを記録する。別環境でruntime不足が出る場合はXcode Settings → Componentsから対応runtimeを導入する。
2. 連続2回目の完了確認保留を再試験し、TX・ACKと残り時間を記録する。不一致が再発した場合もログを保存する。機種・OS・実行scheme・清掃設定と成功時のログも記録する。
3. [実機試験表](HARDWARE_VALIDATION.md)を販売品と同じ構成で実施し、各公開対象経路を2機種以上で各10回連続成功させる。
4. 実測した時間上限と対応端末・OSを記録し、`check_release_readiness.py`を通す。現状は記録がないため意図どおり失敗する。
5. App Store向けには上記の実測記録を揃えて審査へ進む。TestFlightの試験配布は別のBeta構成を使い、
   [配布手順](DISTRIBUTION.md)に従って署名・テスト情報・必要な審査を準備する。

通常ビルドの表示書き込み、4階調NFC書き込み、未検証の4階調からの移行は無効です。
編集・保存・BIN入出力・URL・STATUSの実装を含みますが、実通信の成功をSimulator試験から推定していません。
